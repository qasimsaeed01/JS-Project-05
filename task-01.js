// Task 1: Movie Ticket Booking System

let userName = prompt("Enter your name: ");
let availableSeats = 30;
let seatNumber = Math.ceil(Math.random() * availableSeats);

alert(`Hello ${userName}, your seat number is ${seatNumber}`);
